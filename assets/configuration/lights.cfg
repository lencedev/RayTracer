camera:
{
    resolution = (800, 600);
    position = (0, 0, 0);
    rotation = (0, 0, 0);
    fov = 90;
    max_depth = 50;
    samples_per_pixel = 100;
}

primitives:
{
    sphere:
    {
        position = (0, 0, 0);
        radius = 1;
        material = "diffuse";
    }
}